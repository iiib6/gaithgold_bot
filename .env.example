# Configured Port
PORT=3000

# PostgreSQL Connection String (Optional: falls back to local JSON if empty)
# DATABASE_URL=postgres://username:password@host:port/database_name

# JSON Web Token (JWT) secret for securing API endpoints
JWT_SECRET=al-ghaith-gold-jwt-secret-2024

# Toggle Localtunnel to expose server online (true/false)
TUNNEL=false
TUNNEL_SUBDOMAIN=algaith-gold-dashboard

# Gemini API Key for chatbot responses
GEMINI_API_KEY=

